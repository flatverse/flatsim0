* Account for corner heights!!! (tile slopes)
* have some sort of draw mode system for easy map wide "swapping" of drawable packs

TilePerspective
* expose a depth shift of some sort maybe through a field that defaults to 2 or something

TileSection
* add support for an onUpdate callback

TileDrawablePack
* add an update so that animated drawables can be updated

TilePerspective
* account for scale in .getTilePixelPosition
* make sure .getCenterCoord is correct